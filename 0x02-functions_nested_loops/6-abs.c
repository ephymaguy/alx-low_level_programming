#include "main.h"
/**
 * _abs - converts to absolute num
 * @c: inpute of the function
 * Return: returns c and 0
 */
int _abs(int c)
{
if (c < 0)
{
c = c * -1;
}
else
{
c = c * 1;
}
return (c);
return (0);
}
