#include <stdio.h>
/**
 * main - prints 0 to 9
 * Return: returns zero at the end of the program
 */
int main(void)
{
int i;

for (i = 0; i < 10; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
