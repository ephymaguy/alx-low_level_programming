#include <stdio.h>
/**
 * main - a program that prints using the function printf
 * Return: returns a value o
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
